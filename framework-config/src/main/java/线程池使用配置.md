

```java
xuegao:
    concurrent:
        enable: true //开启为true，不开启则设置为false
        以下都有默认值，可以不设置
        corePoolSize: 16 //线程池核心线程数
        maximumPoolSize: 16 //线程池最大线程数
        keepAliveTime: 30 //超过核心线程数的线程的存活时间
        blockingQueueCapacity: 1024 //阻塞队列容量
        blockingQueueType: ARRAY_BLOCKING_QUEUE //阻塞队列类型
        concurrentActive: true //是否使用线程池并发执行查询

```